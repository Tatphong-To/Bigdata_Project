# Pinned loosely for now; tighten once the pipeline is running.

# --- Data extraction / pipeline (Phase 1-3) ---
httpx>=0.27,<0.28
tenacity>=8.5,<9            # retry/backoff for API calls within the rate limit
psycopg[binary]>=3.2,<4     # Postgres driver for pipeline + service
SQLAlchemy>=2.0,<2.1
pandas>=2.2,<2.3
numpy>=1.26,<2.2

# --- ML (Phase 3 / Layer B, Airflow only) ---
scikit-learn>=1.5,<1.6      # K-Means — trained in Airflow, never in the request path
mlflow>=2.17,<2.18          # experiment tracking, Postgres backend store

# --- Airflow (Phase 3) ---
# Airflow itself is provided by the apache/airflow image in docker-compose.
# Install locally only for DAG unit tests / parse checks:
apache-airflow==2.10.3

# --- Model Service (Phase 5) ---
fastapi[standard]>=0.115,<0.116
pydantic>=2.9,<3
uvicorn>=0.32,<0.33

# --- Dev / test ---
pytest>=8.3,<9
pytest-cov>=5,<6
ruff>=0.7,<0.8
respx>=0.21,<0.22          # mock httpx calls in tests (no real API hits)
